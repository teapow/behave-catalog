# -*- coding: utf-8 -*-
from __future__ import absolute_import, unicode_literals

# Keys.
PHRASE = "phrase"
DOCSTRING = "docstring"
SOURCE = "source"
FILE = "file"
LINE = "line"
FUNC = "func"

# Step types/
GIVEN = "given"
WHEN = "when"
THEN = "then"
STEP = "step"

STEP_TYPES = [
    GIVEN,
    WHEN,
    THEN,
    STEP,
]

# Resources.
STATIC_FILES = [
    "bootstrap.min.css",
    "bootstrap.min.js",
    "index.css",
    "index.html",
    "index.js",
    "jquery.debounce.min.js",
    "jquery.min.js",
    "popper.min.js",
]

INDEX_FILE = "index.html"
